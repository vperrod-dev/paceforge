# Session handoff — Plan-Quality Upgrade shipped (2026-07-08)

## What happened
Victor's three complaints (plans boring / descriptions thin / not Garmin-enhanced)
→ researched (Runna + competitors, repo deep-map), planned (v1), hardened through a
three-review round (coach science / engineering-vs-code / Garmin feasibility, v2),
then **fully executed: 10 PRs on master, 320 tests green**.

Plan + full spec: `tasks/plan-quality-upgrade-2026-07-08.md` (v2, marked SHIPPED with
per-PR commit hashes).

## Commit map (master)
| Commit | Content |
|---|---|
| `7f4963d` | PR1 — percentage pace bands (`TrainingPaces.band`), `pace_key` on steps, `pace_bands` on plan |
| `8fc8077` | PR2 — structural soundness: volume anchored to actual mileage (ACWR), peak ≤2.2× current, goal-feasibility gate (`predict_time`), race pace from target_time, skeleton density at ≤3 days, frequency-scaled long-run cap, long run = volume dial |
| `e07a560` | PR3 — `variants.py` (Canova-lever tables, volume-gated, deload step-back), new builders (speed_reps/ladder/progressive_tempo/alternations/steady_m), phase-correct adjacency-safe pools, consecutive-week variety validate rule, plan.yml prompt trim |
| `8958047` | PR4 — rule-driven long-run rotation (4 subtypes, rehearsal 3wks out, taper easy), `long_run_blocks` + `time_trial` builders, TT on deloads strips Q2, strides before Q1 |
| `d3f5c19` | PR5/6 — `briefings.py` (8-slot coaching briefing per session + week intros), `Workout.briefing`/`TrainingWeek.intro`, `paceforge plan-md`, portal briefing render, SKILL.md judgement-layer rewrite |
| `1106808`/`0b6cc08` (merge `6ababcc`) | PR7/8 (worktree agent) — Garmin: ACTIVE/REST step ids, band-based zero-width widen, per-step notes ≤200 chars, HR bpm targets, cap 500; `autosync.yml` Mondays 06:00, `paceforge autosync`, **push.yml persistence fix** (was discarding garmin_workout_id in CI — live dedup bug) |
| `fb7d961` | PR9/10 — pace insights in `compliance.py` (heat-adjusted fastest-N splits vs window, HR guard, ≥3-week 5-session status), `paceforge recalibrate` + `recalibrate.yml` + portal chip/accept button (guards: ±1 VDOT, 1/4wks, frozen final 3wks, readiness-gated, future weeks only) |
| `6709cf4` | briefing purpose → watch description; plan.yml runs `paceforge plan-md`; watch-settings hint in portal |
| `5096587` | display font Space Grotesk → Bricolage Grotesque; dead width transition dropped (both verified live on Pages) |
| (this commit) | docs: README + CLAUDE.md updated to the new engine; this handoff |

## OPEN — needs Victor
1. **Regenerate the plan** (portal Create Plan) — active plan predates the new engine.
2. **Phase 0 watch confirm** (only he can): push a week, check each step shows a
   min/km pace RANGE, step notes render, transitions beep, intervals group. Watch
   settings: auto-lap OFF, auto-pause OFF, audio prompts ON.
3. First `autosync.yml` run next Monday 06:00 UTC — verify it pushed 2 weeks + cleaned stale.
4. Pace-insights chip appears after ~5 completed quality sessions spanning ≥3 weeks.

## Gotchas for next session
- `garminconnect` is a pinned fork (rebase, never pip bump); extra="allow" is how raw
  Garmin JSON fields are attached (pace.zone, description, HR targets).
- plan.md is now generated by `paceforge plan-md` — never hand-written (skills updated).
- push.yml/autosync.yml commit plan.json back; anything else touching plan.json in CI
  must rebase-safely (concurrency group `paceforge-data`).
- Design hook (impeccable) findings on web/index.html resolved this session; portal
  display font is now Bricolage Grotesque.
- Verify from Victor's vantage: Pages CDN lags 3-4 min; hard-refresh.
